export default (theme) => ({
  root: {
    width: '100%'
  },
  button: {
    marginTop: theme.spacing(1),
    marginRight: theme.spacing(1)
  },
  actionsContainer: {
    marginBottom: theme.spacing(2)
  },
  resetContainer: {
    padding: theme.spacing(1)
  },
  box: {
    margin: theme.spacing(0, 2)
  },
  sideSalesType: {
    position: 'fixed',
    top: '20%',
    right: '0'
  },
  category: {
    margin: theme.spacing(0.4, 'auto')
  }
});
